# ================================================================
#  DEVELOPMENT DEFAULTS — safe for local use, not for production
# ================================================================

# ── Database ─────────────────────────────────────────────────────
POSTGRES_USER=attendance
POSTGRES_PASSWORD=attendance_dev_pass
POSTGRES_DB=attendance_db

# ── Redis ────────────────────────────────────────────────────────
REDIS_PASSWORD=redis_dev_pass

# ── Backend ──────────────────────────────────────────────────────
# DO NOT use these keys in production — generate fresh ones (see README)
SECRET_KEY=dev-secret-key-change-this-in-prod-min32chars-x
ENVIRONMENT=development
FACE_ENCRYPTION_KEY=rS_E8ngP50yrvbXJOOb6rIU4D-CGrsbjzWkC41GIngw=
CONFIDENCE_THRESHOLD=0.50
COOLDOWN_MINUTES=5
EVENT_RETENTION_DAYS=90

# ── Edge node ────────────────────────────────────────────────────
TENANT_ID=demo
DEVICE=cpu
# Empty = no token required in development mode
EDGE_TOKEN=

# ── Frontend (routed via nginx on port 80) ───────────────────────
NEXT_PUBLIC_API_URL=http://localhost
NEXT_PUBLIC_WS_URL=ws://localhost
NEXT_PUBLIC_EDGE_URL=
